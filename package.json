{
  "name": "ocr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Open Culinary Runtime — core schema and validation engine",
  "scripts": {
    "validate": "tsx scripts/validate.ts",
    "validate:entities": "tsx scripts/validate-entities.ts",
    "build": "tsc -p ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
